# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more details: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB, and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings
#
# Replace "myuser" with your system username. You can check your current username by running:
# echo $USER  (on macOS/Linux)
# echo %USERNAME%  (on Windows)
DATABASE_URL="postgresql://myuser:mypassword@localhost:5432/mydatabase"

# Auth Secret (automatically generated via `pnpx auth secret`)
AUTH_SECRET="" # Run `pnpx auth secret` to generate a secure value. Read more: https://cli.authjs.dev

# OAuth Credentials
AUTH_GITHUB_ID={CLIENT_ID}
AUTH_GITHUB_SECRET={CLIENT_SECRET}

AUTH_APPLE_ID={CLIENT_ID}
AUTH_APPLE_SECRET={CLIENT_SECRET}

AUTH_GOOGLE_ID={CLIENT_ID}
AUTH_GOOGLE_SECRET={CLIENT_SECRET}
